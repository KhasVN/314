import { EmployerLoginPage } from '../../../features/employer-login/employer-login-page';

export const metadata = { title: 'Employer Sign In | TalentMatch' };

export default function Page() {
  return <EmployerLoginPage />;
}
