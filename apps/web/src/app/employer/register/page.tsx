import { EmployerRegisterPage } from '../../../features/employer-register/employer-register-page';

export const metadata = { title: 'Employer Register | TalentMatch' };

export default function Page() {
  return <EmployerRegisterPage />;
}
