import { EmployerRegisterPage } from '@features/authentication';

export const metadata = { title: 'Employer Register | Linkedout' };

export default function Page() {
  return <EmployerRegisterPage />;
}
