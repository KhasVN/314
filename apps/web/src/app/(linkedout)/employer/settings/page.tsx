import { EmployerSettingsPage } from '@features/employer/pages/employer-settings-page';

export const metadata = { title: 'Account Settings | Linkedout' };

export default function Page() {
  return <EmployerSettingsPage />;
}
