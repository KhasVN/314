import { CandidateHome } from '@features/candidate';

export const metadata = {
  title: 'Find Your Next Job | Linkedout',
  description: 'Browse and search job opportunities matched to your profile',
};

export default function Page() {
  return <CandidateHome />;
}
