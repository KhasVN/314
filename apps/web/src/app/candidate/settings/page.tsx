import { SettingsPage } from '../../../features/candidate-settings/settings-page';
export const metadata = { title: 'Account Settings | TalentMatch' };
export default function Page() { return <SettingsPage />; }
